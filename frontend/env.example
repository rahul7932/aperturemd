# Backend API origin (optional).
# Set in production (e.g. Railway) so the frontend can call the backend.
# No trailing slash. Example: https://your-backend.railway.app
# Leave unset in local dev to use the Vite proxy (localhost:8000).
# VITE_API_BASE_URL=
